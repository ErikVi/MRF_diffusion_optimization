# Run: mrf-legacy-phantom --config experiments/undersampling/default.toml
# Omitted settings retain the documented historical defaults; inspect with --dry-run.
input_directory = "../../data/input"
output_directory = "../../data/output/undersampling"
